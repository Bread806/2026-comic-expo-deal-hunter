# 交接文件：2026 漫博免費好禮地圖網站

> 交接目的：本 agent（glm-5.2）不支援 image input，無法以 vision 確認 `assets/map.jpg` 的視覺事實。請具備 vision 能力的 agent 接手，完成「地圖數位化方式」的視覺確認與後續實作規劃。

---

## 專案概要

- **目標**：台灣 2026 漫畫博覽會「免費好禮地圖」網站
- **功能**：在攤位地圖上標示哪些攤位有免費獎品可拿
- **三色標示**：
  - 綠色 = 免費拿取（無條件）
  - 黃色 = 有條件免費拿取（集章、訂閱等）
  - 灰色 = 無免費內容或未更新
- **編輯模型**：任何人可自由編輯、免登入、可署名可匿名
- **主要載具**：手機，需確保使用友善且不跑版
- **部署**：GitHub Pages（前端）+ 外部後端
- **時程**：只剩一天完成
- **預算**：必須全免費
- **開源精神**：方便大家使用

---

## 已確認的決策

| # | 決策 | 結論 |
|---|------|------|
| Q1 | 編輯的資料持久化 | **(B) 外部後端 / DB** — 違反純 GitHub Pages，但滿足「自由即時編輯」核心需求 |
| Q2 | 後端服務 | **Supabase Free Tier** — Postgres + RLS + Realtime + Edge Functions；勝過 Firebase Spark 的 20k writes/day 硬上限（多人編輯+惡意刷量會撞牆） |
| Q3 | 防濫用模型 | **(C) 直接覆寫 + history 表 + Edge Function IP 限頻** — 保留匿名即時編輯 UX，限頻擋 bot，history 為安全網可還原 |
| Q4 | 攤位清單建立權限 | **(A) 管理員預先定義** — 攤位清單來自你手上的官方地圖，固定 122 個；匿名使用者只能對既有攤位編輯獎品/條件/狀態，不能新增或刪除攤位 |
| Q5 | 攤位 PK 識別 | **(D) `booth_id` 序號 PK + `name` 顯示 + `bbox` 渲染座標** — 因地圖不規則，無官方編號，座標 PK 不適用，改用序號；名稱供遊客辨識，bbox 供前端定位 |

### Q1-Q5 決策依賴鏈
- Q1=B → 必須選後端（Q2）
- Q2=Supabase → RLS/Edge Function 可用（Q3 選 C 的前提）
- Q3=C 即時覆寫 → 攤位清單必須固定以防灌爆（Q4 選 A）
- Q4=A 攤位預先定義 → 需要 PK 方案（Q5）
- Q5=D → PK 與 bbox 欄位已定，但 bbox 來源依賴 Q6 的數位化方式

---

## 待確認：Q6 地圖的數位化方式

### 背景事實（來自使用者口頭描述，未經 vision 驗證）
- `assets/map.jpg` 為 7016×9921 大 JPEG（PS 製成）
- 場地為 L 型/不規則多邊形，非整齊行列格
- 攤位大小不一，有大型連續色塊（B舞台/A舞台/大會服務台/贈品區）
- 格內為廠商中文名稱（部分中英夾雜、部分為 logo 圖示）
- 含服務設施：洗手間、ATM、電梯、售票亭、醫務室、警衛室
- 走道系統複雜：中央主走道、四周環繞動線、攤位間窄走道
- 攤位數約 122 個

### 候選方案
| 方案 | 說明 | 適配度 |
|------|------|--------|
| (A) CSS Grid + span | 用 grid-column/row-span 模擬大小不一 | 對 122 攤+舞台+服務設施+走道太勉強，手機易跑版。**已撤回推薦** |
| **(B) JPG 背景 + 絕對定位熱點** | 原圖為背景，每攤用 `(x%, y%, w%, h%)` 疊可點 div | **推薦**。展場地圖標準做法，最貼近原圖，手機 pinch zoom 看細節 |
| (C) SVG 重繪 | vector 重畫 | 一天做不完。排除 |
| (D) Image map `<area>` | HTML 原生 | 手機支援差、樣式難調。排除 |

### 本 agent 的暫定推薦：(B)
**理由**：
1. 不規則地圖用絕對定位最忠實，不跑版
2. 原圖 7016×9921 高解析，當背景夠用
3. 色塊（綠/黃/灰）= 熱點 div 的半透明 background 疊在原圖上
4. 攤位名稱直接用原圖上的，省打字——熱點只負責定位＋著色＋點擊

### 配套工作流（若採 B）
1. 實作階段先做「標座標小工具網頁」：載入原圖、滑鼠拖曳框選每攤、輸入名稱、自動匯出 JSON
2. 你用工具標完 122 攤 → 匯出 JSON
3. JSON 匯入 Supabase 作為 seed

---

## 待 vision agent 確認的事項

請用 vision 檢視 `assets/map.jpg` 並回報：

1. **整體布局**：是否為不規則多邊形？場地輪廓形狀？
2. **攤位格形狀**：大小是否一致？有無 L 型/直式長條/大型連纉色塊？
3. **攤位標籤**：格內是否為中文廠商名稱？是否可辨識？
4. **攤位數量**：實際大約幾個攤位格？（使用者估 122）
5. **非攤位區域**：舞台、服務台、走道、服務設施（洗手間/ATM 等）的分布？
6. **多區域/樓層**：是單一展館還是多區/多樓層？
7. **既有色彩編碼**：地圖本身是否已用顏色標示意義？
8. **方位/圖例**：有無 compass、scale、legend？
9. **數位化方式建議**：基於以上事實，(A) CSS Grid + span / (B) JPG 背景 + 絕對定位熱點 / 其他，哪個最適合？理由？

---

## 已排除的選項（供接手 agent 参考，勿重走）

- **Q1 (A) 純前端 + GitHub 機制收編輯**：即時性不足，使用者明確要 (B)
- **Q1 (D) 不做持久化**：違反「自由編輯」需求
- **Q2 Firebase Firestore**：20k writes/day 硬上限，多人編輯+惡意刷量會撞牆
- **Q3 (B) 提案制**：違反「方便用、自由編輯」精神
- **Q4 (B)(C) 開放新增攤位**：與官方地圖不一致、被亂加幽靈攤位、攻擊面擴大
- **Q5 (C) 名稱 PK**：撞名、改名時歷史編輯斷裂
- **Q6 (C) SVG 重繪**：一天做不完 122 格
- **Q6 (D) Image map**：手機支援差

---

## 已知環境事實

- Repo: `/Users/bread/Documents/code/Github/2026-comic-expo-deal-hunter`
- Git: 已 init、已設 remote `git@github.com:Bread806/2026-comic-expo-deal-hunter.git`、main 分支已 push、有一個 "first commit" 含 README.md
- 唯一檔案：`README.md`（內容 `# 2026-comic-expo-deal-hunter`）、`assets/map.jpg`（5.4MB, 7016×9921, PS 製成）
- 本 agent 模型：glm-5.2，**不支援 image input**

---

## 接手 agent 的下一步

1. 用 vision 檢視 `assets/map.jpg`，回報上述 9 項事實
2. 與使用者確認 Q6 的地圖數位化方式（預期會定案為 B）
3. 定案後接著走的決策樹（尚未問）：
   - **Q7**：獎品/條件的資料結構（一攤多獎品？條件如何表達？）
   - **Q8**：編輯表單的欄位設計（要填什麼、必填可選、署名欄位）
   - **Q9**：地圖主介面的 UX（清單 vs 地圖優先、搜尋、篩選色塊）
   - **Q10**：Supabase schema 與 RLS policy 細節
   - **Q11**：Edge Function 限頻規則（頻率、IP 來源、例外）
   - **Q12**：前端技術棧（vanilla / Vite + 框架？手機優先 CSS strategy）
   - **Q13**：部署流程（GitHub Pages build、Supabase env、secrets）
   - **Q14**：種子資料流程（標座標工具 → JSON → Supabase seed）
   - **Q15**：管理員介面（你如何還原 history、檢舉處理）
4. 走完決策樹後產出實作計劃，再進入實作

---

## 建議 skills

接手 agent 可考慮載入：
- `grilling` — 繼續逐題訪談至共識
- `prototype` — Q6 定案後可做標座標工具的 throwaway prototype
- `to-spec` — 決策完成後可產出 spec
- `to-tickets` — 拆解實作 ticket
- `implement` — 實作階段